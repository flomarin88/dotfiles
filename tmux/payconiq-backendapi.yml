# ~/.tmuxinator/payconiq-backendapi.yml

name: payconiq-backendapi
root: ~/dev/PAYCONIQ/backendapi

windows:
  - editor:
      layout: 9544,181x45,0,0[181x12,0,0,18,181x32,0,13,20]
      panes:
        - while true; do clear; glog -10 | cat; sleep 5; done
        - 
  - logs:
      - cd ../payconiq-deployer
      - vagrant ssh
      - tail -f /var/log/tomcat/log/app/payconiq/main.log
